name: response_x
description: library for exchanging response between server and client. it can be a useful tool for standardizing the way that a server communicates with its clients.
version: 1.0.0
homepage: https://github.com/ravikovind/response_x
issue_tracker: https://github.com/ravikovind/response_x/issues

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  equatable: ^2.0.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
